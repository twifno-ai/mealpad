# Mealpad（家餐）— 家庭点菜与餐历管理

**项目名称**：**Mealpad**（英文：meal + pad，意为「餐食记事本」，English-friendly）  
**中文名**：家餐  
**仓库名**：`mealpad`（推荐），或 `mealpad-app`

## 问题定义

家庭做饭时经常面临「今晚吃什么」「冰箱里缺什么」「菜谱记在哪」的困扰。需要一款面向家庭的点菜与菜谱管理工具：能保存菜谱、按周/日安排吃什么、并根据餐历自动生成采购清单，减少重复决策与漏买食材。

## 用户目标

1. **菜谱管理**：添加、编辑、删除菜谱；每条菜谱包含名称、食材列表（含用量与单位）、步骤、份量（几人份）、标签（如「快手」「素食」）。
2. **点菜/餐历**：为某一天或某一餐指定要做的菜谱（从已有菜谱中选择），支持查看本周/本月的餐历；并能**按日期搜索与查看历史记录**（某天/某段时间吃了什么）。
3. **采购清单**：根据选定日期范围内的餐历，自动汇总所需食材并支持按类别或按菜合并显示；可勾选「已采购」、手动增删项。
4. **份量缩放**：在餐历或菜谱中调整份量时，食材用量按比例自动换算。
5. **数据本地优先**：数据存储在本地（单机或家庭共享目录），无需强制登录云端；可选导出/导入备份。
6. **足够好用的 UI**：提供清晰、易用的界面（Web 或桌面端），菜谱、餐历、采购清单、历史记录等主要功能在少量点击内可达；信息层次分明。**尤其照顾手机用户**：在手机上浏览、点菜、查餐历、勾选采购项等操作要方便（大按钮、少误触、竖屏友好），适合家庭日常在手机/平板/电脑上使用。

## 非目标

- 首版不做多用户账号与云端同步；不做从网页抓取菜谱的「剪藏」功能；不做社交分享与评论；不做营养计算或卡路里分析。

## 验收标准（Checklist）

- [ ] **菜谱 CRUD**：支持新增、编辑、删除、按名称/标签搜索菜谱；菜谱含名称、食材（名称+用量+单位）、步骤、默认份量、标签。
- [ ] **餐历**：可为指定日期（及可选餐次：早/午/晚）分配菜谱；支持查看某周或某日的餐历；同一餐可多道菜。
- [ ] **历史记录**：支持按日期或日期范围搜索、查看「每天吃什么」的历史（已记录的餐历），便于回顾与复做。
- [ ] **采购清单**：选择日期范围后，根据该范围内餐历中的菜谱及份量，自动汇总食材并去重/合并；清单可编辑（勾选已购、增删项）；支持按类别或按菜分组显示。
- [ ] **份量缩放**：菜谱或餐历中修改份量时，该菜谱下食材用量按比例自动更新显示。
- [ ] **持久化**：菜谱、餐历、采购清单状态持久化到本地（如 SQLite 或 JSON 文件），重启后不丢失。
- [ ] **界面（UI）**：主要功能（菜谱浏览/编辑、点菜、查看餐历、生成采购清单、查历史）在界面中易发现、少步骤完成；布局与信息层次清晰；支持常见分辨率与触屏操作；若为 Web，支持在局域网内多设备访问。
- [ ] **手机端体验**：在小屏（如手机竖屏）下布局自适应、文字与可点击区域足够大（避免误触）；核心操作（点菜、勾选已购、切换餐历/历史）支持触控且步骤少；关键按钮与导航在拇指可及范围内或便于单手操作。
- [ ] **入口**：提供 Web 或桌面端作为主入口；可额外提供 CLI 辅助。若为 Web，支持在局域网内同一家庭设备访问。
- [ ] **局域网多设备访问说明**：产品交付时须提供「如何在局域网内多设备访问」的说明（含服务绑定地址、端口、本机 IP 查询、他机访问 URL、常见防火墙/路由器注意点），便于家庭成员在手机、平板、电脑上通过同一 WiFi 访问。
- [ ] **测试**：核心逻辑（菜谱解析、餐历聚合、清单生成、份量计算）有单元测试覆盖。

## 约束条件（性能 / 安全 / 成本）

- **性能**：餐历与清单生成在 500 条菜谱、90 天餐历量级下应在 3 秒内完成。
- **安全**：不执行用户输入中的外部命令；若支持导入文件，仅解析指定格式（如 JSON），不做任意代码执行。
- **成本**：首版无付费 API 依赖，可完全离线使用；若后续增加「从 URL 抓菜谱」等能力再单独评估。
